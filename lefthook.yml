# Pre-commit runs the SAME gate as CI (scripts/verify.ts) — identical checks,
# no drift by construction. If it's green here, it's green there.
pre-commit:
  commands:
    verify:
      run: pnpm verify
